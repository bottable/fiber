import { Locale } from '../LocaleProvider';


const localeValues: Locale = {
  locale: 'en_US',
  ExampleComponent: {
    welcome: "welcome",
    subText: "subText",
  }
}

export default localeValues;
